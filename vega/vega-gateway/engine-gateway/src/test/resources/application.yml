server:
  port: 8099
  max-http-header-size: 512KB
  tomcat:
    accept-count: 1000
    max-threads: 800
    min-spare-threads: 100
spring:
  main:
    allow-circular-references: true
  application:
    name: vega-gateway
    version: 1.0.0
    queryName: vega-calculate-coordinator

  mvc:
    throw-exception-if-no-handler-found: true
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  datasource:
    dynamic:
      primary: mysql  # 指定默认数据源
      strict: true # true：找不到数据源报错
      lazy: true
      datasource:
        mysql:
          type: com.alibaba.druid.pool.DruidDataSource #引入自定义的Druid数据源
          driverClassName:
          url: jdbc:fake://
          username: xxx
          password: xxx
        clickhouse:
          type: com.alibaba.druid.pool.DruidDataSource #引入自定义的Druid数据源
          driverClassName:
          url: jdbc:fake://
          username: xxx
          password: xxx

#mybatis-plus配置
mybatis-plus:
  global-config:
    banner: false
    enable-sql-runner: true
  mapper-locations: classpath*:com/**/mapper/xml/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl

#openlook相关配置
openlookeng:
  url: http://127.0.0.1:8090
  connect-timeout: 3000
  jdbc:
    pushdown-module: FULL_PUSHDOWN
olk:
  view:
    catalog: olk_view_vdm
  connector:
    name: mysql,maria,hive-hadoop2,postgresql,clickhouse,doris,oracle,sqlserver,hive-jdbc

sql:
  enabled: true

#AF权限相关配置
af-auth:
  is-open: true
  register-clientid-url: http://127.0.0.1:31125/admin/clients
  clientid-token: http://127.0.0.1:31125/admin/clients
  token-introspect: http://127.0.0.1:31125/oauth2/introspect
  row-column-rule: http://127.0.0.1:31125/admin/clients


cache-manager-service:
  X-Presto-User: admin
  catalog-cache-name: mf_clickhouse_cache
  catalog-cache-database: cache_database


collector-data-source: mymaria_fast2 #扫描用数据源，列表过滤时使用

services:
  efast-public: http://10.4.110.188:9123